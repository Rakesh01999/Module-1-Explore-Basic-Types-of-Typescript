// Basic Data Type

// String
let fname: string = "Rakesh"

// Number
let roll: number = 12

// Boolean
let isAdmin: boolean = true

// undefined
let x: undefined = undefined

// null
let y: null = null





let d : number;

d = 123
// d = "Hello"  // if previously type given then it'll show wrong
// d = false    // if previously type given then it'll show wrong


// Array

let friends: string[] = ['Rasel', 'Monica']

let numbers: number[] = [12, 13, 14]

// numbers.push('dd') // wrong , because previously it is said that, numbers will be number[] .

// tuple --> array --> order --> type of values

let conordinates: [number, number] = [4, 5]

let nameAge: [string, number, boolean] = ["Goni", 55, true]

nameAge[0] = "neo"   // ok
// nameAge[0] = 345  // not ok


